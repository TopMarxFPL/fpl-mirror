# fpl
Fantasy Premier League data, fetched directly from the official FPL API. Data is available as raw JSON (exactly as returned by the API) and as processed CSVs for easy use in spreadsheets, notebooks, and analysis tools.
